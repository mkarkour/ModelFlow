# 📝 EXERCISE: Documentation & Testing (Data Vault)
# 
# TASK: 
# For every model you create in your exercise folder, add an entry below.
# 
# 💡 DBT PACKAGE TIP: 
# When using packages like `dbt_utils`, you can reference their macros 
# in your SQL (e.g., `{{ dbt_utils.generate_surrogate_key(...) }}`). 
# To use package-specific tests in this YAML, simply prefix the test name 
# (e.g., `dbt_utils.at_least_one`).
#
# 🛠️ CODEGEN TIP:
# You can use the `dbt-codegen` package to automatically generate 
# the YAML boilerplate for your models! Run:
# dbt run-operation generate_model_yaml --args '{"model_names": ["your_model_name"]}'
#
# 🌟 BONUS POINT:
# Add at least two tests (unique, not_null, or relationship) for each model!

version: 2

models: []
  # - name: your_model_name
  #   description: "Describe what this model does"
  #   columns:
  #     - name: hk_customer
  #       tests:
  #         - unique
  #         - not_null
